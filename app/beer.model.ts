export class Beer {
  public low: boolean = false;
  constructor(public nameOfBeer: string, public id: number, public brand: string, public price: number, public alcoholContent: number, public pintsRemaining: number) {

  }
}
